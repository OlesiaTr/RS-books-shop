const refs = {
  main: document.querySelector("main"),
};

const galleryContainer = new DocumentFragment();
const galleryList = document.createElement("ul");

export { refs, galleryContainer, galleryList };
